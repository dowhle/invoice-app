export const selectCustomerList = (state) => state.customer.list;
export const selectCustomerDetail = (state) => state.customer.detail;

export const selectProductList = (state) => state.product.list;
export const selectProductDetail = (state) => state.product.detail;

export const selectInvoiceList = (state) => state.invoice.list;
export const selectInvoiceDetail = (state) => state.invoice.detail;